#include <iostream>

int main()
{
    char a = '7'; // there are total signed -128 to 127 cahracters and 256 unsigned  characters
    std::cout << a << '\n';
    char b = 44; // there are asci codes for all the characters it can store.
    std::cout << b << '\n';

    // char can also store num value but it is highly avoided.
    return 0;

}