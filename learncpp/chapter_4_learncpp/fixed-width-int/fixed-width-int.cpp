#include <iostream>
#include <cstdint> // for fixed-width int types

int main() 
{
    std::int8_t a {65}; // these behaves as char data type
    std::uint8_t {256}; // u is for unsigned
    std::int16_t b {343434};
    // there is also int32 and int 64 versions also.
    return 0;
}

