{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "(gdb) Debug C++ (Git Bash)",
      "type": "cppdbg",
      "request": "launch",
      "program": "${fileDirname}/${fileBasenameNoExtension}.exe",
      "args": [],
      "stopAtEntry": false,
      "cwd": "${fileDirname}",
      "environment": [],
      "externalConsole": false,
      "MIMode": "gdb",
      "miDebuggerPath": "gdb.exe",
      "preLaunchTask": "Build C++ (g++)" // <-- This name must match the "label" in tasks.json
    }
  ]
}